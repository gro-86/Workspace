#spring.application.name=msalumnos #Cambia el nombre a la app.
#server.port=8081 #
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
vspring.datasource.url=jdbc:mysql://localhost:3306/?serverTimezone=UTC
#spring.datasource.username=root
#spring.datasource.password=Groverone1986_
#spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
#spring.jpa.generate-ddl=true
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.properties.hibernate.show_sql=true
#spring.jpa.properties.hibernate.use_sql_comments=true
#spring.jpa.properties.hibernate.format_sql=true 

    #server.port=8081: Specifies the port on which the embedded Tomcat server will run the application (port 8081 in this case).

    #spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver: Specifies the MySQL JDBC driver class name for database connectivity.

    #spring.datasource.url=jdbc:mysql://localhost:3306/?serverTimezone=UTC: Defines the URL for connecting to the MySQL database running on localhost, port 3306, and sets the server timezone to UTC.

    #spring.datasource.username=root: Sets the username for the MySQL database connection (root in this case).

    #spring.datasource.password=Groverone1986_: Sets the password for the MySQL database connection.

    #spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect: Specifies the Hibernate dialect for MySQL 8, which helps Hibernate generate appropriate SQL queries.

    #spring.jpa.generate-ddl=true: Tells Spring Boot to generate database schema (DDL) based on the entity classes.

    #spring.jpa.hibernate.ddl-auto=update: Controls how Hibernate handles the database schema. "update" means that Hibernate will update the schema if necessary based on the entity classes.

    #spring.jpa.properties.hibernate.show_sql=true: Configures Hibernate to print SQL statements to the console, which can be useful for debugging.

    #spring.jpa.properties.hibernate.use_sql_comments=true: Enables the use of SQL comments in the generated SQL statements. This can help identify which query corresponds to which part of your code.

    #spring.jpa.properties.hibernate.format_sql=true: Formats the SQL statements generated by Hibernate for better readability when printed to the console.